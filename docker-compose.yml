version: "3.9"

services:
  main_server:
    depends_on:
      - redis
    build: ./
    # image: web_dl-main_server:latest
    env_file:
      - .env
    ports:
     - "9000:9000"
    restart: always
    volumes:
      - ".:/app"
  redis:
    image: "redis/redis-stack-server:latest"
